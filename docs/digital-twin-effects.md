# 数字孪生特效组件

特效库按五张示意图的能力组织。空间、模型和环境特效使用场景中的 `poiEffect` 组件；报警、立标、点击事件和巡检入口复用项目已有组件。库中的副标题会说明实际创建哪种组件。示意图用于说明效果意图，最终画面还受模型结构、场景灯光、相机和参数影响。

## 创建、编辑和保存

1. 将特效库卡片拖入场景，选中生成的实体。
2. 在 Inspector 的“特效”面板中设置类型、启用状态、颜色和该效果支持的参数。建筑外观特效可绑定“环境模型”、真实模型或基础网格；逻辑文件夹不能作为绑定目标。
3. 路径和区域使用实体局部坐标，每行填写 `X, Y, Z`，单位为米。通过 Transform 控制整体位置、旋转和缩放。区域自动闭合；路径不自动闭合。
4. 编辑多行坐标、数值或标签后点击“应用区域轮廓”“应用路径”或“应用空间数据”。校验失败时保留输入草稿并显示错误，不写入场景。调整颜色等其他参数不会清除尚未应用的草稿。
5. 保存场景后，组件配置随场景保存。场景中的编辑和应用操作进入既有撤销 / 重做流程。锁定实体时，配置控件不可修改。
6. 在运行预览检查模型动画、数据交互和镜头行为。更新已发布项目时，需要重新发布场景和匹配的 Viewer 资源，已有发布包不会自动取得新的运行时代码。

原有光墙围栏保持专用面板，使用 `X, Z` 二维轮廓输入；其余新版路径和区域使用 `X, Y, Z`。不要混用两种输入格式。

### 绑定环境模型

- 添加建筑外观特效后，在“绑定目标”中选择“环境模型”，会直接作用于当前环境 GLB 的可渲染几何。
- 也可打开“场景属性 → 环境属性 → 建筑物特效”，从特效库拖入卡片或通过快捷列表添加。该入口创建并选中普通特效实体，自动绑定环境，与上方下拉选择使用同一份配置；已有绑定可通过“配置”按钮打开。
- 支持轮廓高亮、建筑棱线、自发光、扫光、高度渐变、全息、X-Ray 和溶解生长这 8 类建筑外观效果。在这些类型之间切换会保留环境绑定。环境整体位置、旋转和缩放仍在环境属性中设置。
- 调整参数不重新加载 GLB。隐藏环境暂停动画；关闭或解绑效果恢复环境当前外观；整体环境透明度与 X-Ray / 全息透明度相乘。当前环境的阴影材质切换后仍保留特效。
- 清除环境时保留绑定并提示尚未配置环境；撤销清除、重新添加或替换环境后自动绑定当前资源。保存、复制、SOURCE/DIST 和 Viewer 均沿用此绑定；旧场景不受影响。已发布场景需重新发布新版 Viewer。

环境绑定回归：`node --test tests/editor/environmentBuildingEffect.integration.test.mjs tests/runtime/environmentBuildingEffect.test.mjs`、`node scripts/smoke-environment-building-effect.mjs`。后者使用真实 GLB 和编辑器界面验证拖放、选择、8 种切换、像素、无额外 GLB 请求、保存重开与清除撤销，输出在 `output/environment-building-effect/`。完成构建后运行 `node node_modules/electron/cli.js tests/digitalTwin/environmentBuildingEffectPackages.integration.mjs` 核对双包资源与绑定，再运行 `node scripts/smoke-environment-building-effect-viewer.mjs` 验证实际 DIST Viewer 中的环境 GLB、材质编译和可见像素。

## 输送方向箭头

参考输送线方向示意图新增六种独立预设：单一直线箭头、连续箭头流向、分段式箭头、宽幅带式箭头、双列前进箭头、高速流动箭头。卡片带轮廓预览，可拖入场景或设备“表面箭头”样式框，详见[输送方向箭头配置和发布](conveyor-arrow-effects.md)。

## 示意图 01：建筑与模型外观

| 编号 | 示意图能力 | 组件 / 入口 | 使用条件 |
| --- | --- | --- | --- |
| 01 | 轮廓高亮 | `model-outline` | 绑定模型或基础网格，设置轮廓颜色和宽度。 |
| 02 | 建筑棱线发光 | `model-edges` | 根据模型原有几何棱线描边；细分网格的结果由模型拓扑决定。 |
| 03 | 自发光与光晕 | `model-emissive` | 将灯带、标识等独立部件作为目标；整个建筑作为目标时影响整个目标。 |
| 04 | 建筑扫光 | `model-scan` | 绑定模型，设置扫描轴向、宽度和周期。 |
| 05 | 高度渐变着色 | `height-gradient` | 在主色与辅助色之间按模型世界高度渐变。 |
| 06 | 全息投影 / 线框模型 | `hologram` | 在目标几何上显示半透明线框。 |
| 07 | 透明透视 / X-Ray | `xray` | 绑定外壳并降低不透明度；内部设备与结构必须原本存在。 |
| 08 | 溶解出现 / 模型生长 | `dissolve` | 默认单次播放；设置生长轴向、时长、显示上限和循环。显示上限 100% 时，完成后恢复完整原始外观；循环播放会在完成后短暂停留再开始下一轮。 |

## 示意图 02：区域、范围与扫描

| 编号 | 示意图能力 | 组件 / 入口 | 使用条件 |
| --- | --- | --- | --- |
| 01 | 发光围栏 / 动态光墙 | `light-wall-fence` | 使用原有专用光墙面板，支持闭合轮廓、高度、透明度和流动速度。 |
| 02 | 区域边界流光 | `boundary-flow` | 按 3–128 个区域轮廓点形成闭合流光边界。 |
| 03 | 区域半透明填充 | `area-fill` | 填充 X/Z 平面多边形；支持凹多边形，不接受自交、折返或共线轮廓。 |
| 04 | 扩散光圈 / 波纹扩散 | `ripple-ring` | 在实体锚点向外扩散光圈。 |
| 05 | 呼吸光圈 | `breathing-ring` | 以周期变化的亮度强调锚点区域。 |
| 06 | 雷达扫描 / 扇形扫描 | `radar-sector` | 新版扇形扫描，半径控制范围。 |
| 07 | 定位光柱 / 光锥 | `light-pillar` | 新版垂直光柱，半径和高度控制体积。 |
| 08 | 半球罩 / 能量罩 | `energy-dome` | 以锚点为中心显示半球范围，半径控制底面，高度控制罩顶。 |

## 示意图 03：路径、物流与流向

| 编号 | 示意图能力 | 组件 / 入口 | 使用条件 |
| --- | --- | --- | --- |
| 01 | 流光路径 | `flow-path` | 使用 2–128 个局部坐标点定义路径。 |
| 02 | 流动箭头 | `flow-arrows` | 新版路径箭头沿配置方向移动。 |
| 03 | 飞线 | `fly-line` | 以配置路径的首尾点生成弧线，高度控制拱高。 |
| 04 | 轨迹拖尾 | `motion-trail` | 绑定目标后记录实际运动轨迹；未绑定时沿配置路径演示。 |
| 05 | 路径逐段点亮 | `path-reveal` | 按路径进度逐步点亮，支持循环和播放一次后停留。 |
| 06 | 管道流动 / 能量流动 | `pipe-flow` | 沿三维折线绘制管线与流光，不自动识别模型内的管道走向。 |

相邻路径点至少间隔 0.001 米，坐标绝对值不超过 100000 米。路径特效只负责展示；创建路径不会自动配置 AGV、输送线或其他设备的控制逻辑。

## 示意图 04：设备状态、交互与空间数据

| 编号 | 示意图能力 | 组件 / 入口 | 使用条件 |
| --- | --- | --- | --- |
| 01 | 状态变色 | `alarm-manager`，特效库“状态变色” | 在报警管理器中配置运行状态或自定义属性、触发值、目标和覆盖颜色。不同状态需按已有监听规则配置。 |
| 02 | 告警呼吸 | `alarm-manager`，特效库“告警呼吸” | 沿用现有遥测报警条件，已命中的目标材质以 1.6 秒周期平滑呼吸；未报警对象不启用呼吸，解除或停止预览后恢复。独立 `breathing-ring` 用于常驻定位光圈。 |
| 03 | POI 悬浮标签 | `chart-marker`，特效库同名入口 | 在图表立标中设置文本、悬浮高度、面向摄像机及数据字段，也可绑定中台大屏。 |
| 04 | 引线标注 | `chart-marker`，特效库同名入口 | 在图表立标中选择“线形”外观并配置位置、悬浮高度和文本。 |
| 05 | 悬停提示 / 点击信息卡 | `click-event-binding`，特效库“点击信息卡” | 使用已有点击 / 货格点击事件与展示图表配置。当前接入的是点击信息卡，单纯鼠标悬停不会自动触发该组件。 |
| 06 | 设备运行联动动画 | 设备模型 Inspector 的参数、遥测绑定及模型脚本 | 复用现有 AGV、堆垛机、RGV、输送线等模型的 MQTT / 数据驱动能力。设备需要相应模型包、资产编号和遥测字段；不另外创建一个脱离设备逻辑的特效。 |
| 07 | 热力图 | `heatmap` | 使用采样点及数值配置显示热区；热区影响半径最小为整体显示范围半径的 8%。 |
| 08 | 区域分级着色 | `region-level` | 每个点表示一个区域中心，对应一个数值；半径控制单区范围。 |
| 09 | 三维数据柱 | `data-bars` | 每个点对应一个数值和可选标签，最大柱高控制可视化比例，柱体宽度独立可调。 |
| 10 | 监控视锥 / 覆盖扇形 | `camera-frustum` | 使用覆盖范围半宽、安装高度和 Transform 旋转表示覆盖方向；不会自动读取摄像机真实参数或视频。 |

热力图、区域分级着色和数据柱接受 1–64 个采样点，点和数值数量必须相等。数值范围为 0–1000000，标签最多 64 项、每项最多 80 字。默认读取场景中的特效配置值；启用详细配置后，可复用 MQTT 快照或中台托管 HTTP，并通过字段映射及稳定记录 ID 的空间数据集更新。业务指标由实际数据源提供，不自动推断。详见 [特效详细配置与数据绑定](effect-configuration-v2.md)。

## 示意图 05：结构展示、镜头动画与环境氛围

| 编号 | 示意图能力 | 组件 / 入口 | 使用条件 |
| --- | --- | --- | --- |
| 01 | 楼层展开 | `floor-expand` | 按原模型子部件高度展开，设置展开间距和进度。需要可区分楼层的模型结构。 |
| 02 | 零件爆炸图 | `explode` | 沿原模型中心展开子部件，设置展开距离和进度。需要独立零件节点。 |
| 03 | 剖切 / 切片显示 | `clip-section` | 设置轴向与切面进度；不自动生成剖切面封口或缺失内部结构。 |
| 04 | 屋顶隐藏 / 遮挡淡出 | `roof-fade` | 优先使用命名屋顶部件，再按最高子网格选择；准确控制时直接绑定屋顶实体。 |
| 05 | 镜头飞行 | `auto-patrol`，特效库同名入口 | 复用自动巡检的航点、镜头姿态、行进时间、停留和播放模式。 |
| 06 | 目标跟随 | `target-follow` | 绑定实际运动目标，设置跟随距离和高度；仅在运行预览 / Viewer 中接管镜头。 |
| 07 | 环境雾 / 距离雾 | `environment-fog` | 场景级效果，配置起雾距离、过渡距离和不透明度。 |
| 08 | 雨雪粒子 | `rain`、`snow` | 分别创建雨粒子和雪粒子，配置局部覆盖半径、高度和粒子密度。 |
| 09 | 烟雾 / 蒸汽 | `smoke-plume` | 新版烟雾可通过颜色和范围调整为蒸汽表现。 |
| 10 | 火焰 / 火花 | `flame`；既有 `sparks` 可独立显示火花 | 新版火焰含上升火星，用于视觉表现；业务告警触发仍通过报警管理器配置。 |
| 11 | 昼夜切换 | `day-night` | 通过灯光和环境亮度变化表现昼夜；进度 0 为白天，1 为夜晚，循环时自动往返。 |
| 12 | 水面波纹 | `water-surface` | 将程序化波纹面放到水池表面；不自动生成真实水体反射、折射或流体模拟。 |

## 模型与运行边界

- 模型效果依赖目标已实际加载。找不到目标时，Inspector 保留原目标 ID 并提示重新绑定，避免默默改绑其他模型。
- X-Ray、剖切、屋顶淡出不能生成模型中不存在的货架、管线和房间。楼层展开和爆炸图不能把一个整体单网格自动拆成业务零件；应先使用具有相应节点层级的模型。
- 模型材质特效需要独立模型节点。编辑、保存和运行加载时，已绑定目标及其所属既有薄实例批次会自动保留或恢复独立模型，避免整批改色；其他模型继续按原规则合批。V2 目标跟随、拖尾等只读取运行姿态，不因绑定主动拆开静态批次。绑定大型批次的材质效果仍会增加独立模型数量和绘制开销。
- 同一目标同时启用多个模型外观或结构效果时，按先绑定顺序生效；禁用或删除当前特效后，下一特效接管。需要叠加时，优先拆分明确的外壳、灯带、屋顶等目标。
- 报警高亮优先于模型材质效果。报警接管时暂时释放该目标的模型覆盖，解除后再恢复仍启用的模型效果；报警期间禁用模型效果不会让报警解除后还原到已释放的材质。
- 环境雾和昼夜是场景级效果。镜头飞行、目标跟随、手动漫游都可能控制相机，应在运行预览按目标操作流程检查相互切换。
- 新版火焰、烟雾、雷达、定位光柱和路径箭头拥有新的稳定类型 ID。界面不再展示旧入口，但旧场景中的旧类型仍可以读取、运行和删除，在类型选择框显示“旧版”。切换到新类型时应用新类型默认值，应重新调整参数。

隐藏的旧类型为 `radar-scan`、`locator-beam`、`fire`、`smoke` 和 `pipeline-flow-particles`。`pipeline-flow-arrows`、`moving-double-arrow` 和 `conveyor-direction` 已恢复特效库入口，与 `flow-arrows` 一起可拖入输送线的“表面箭头”样式框；该拖放只替换模型配置，不新建特效实体。保留兼容读取不会批量删除用户场景内已有实体。

自定义 Shader / Node 材质保留原样；模型材质效果当前支持 Standard / PBR / MultiMaterial。

## 验证说明

本说明是能力与操作映射，不替代实际场景验收。类型检查、配置单测和浏览器测试分别验证各自覆盖范围，不能据此推断所有外部模型层级、生产 MQTT、线上中台大屏或已部署 Viewer 均已验收。需要发布后验证的场景，应使用真实模型与实际业务数据重新检查保存 / 重开、运行预览和发布播放路径。

### 本地回归命令与记录

```powershell
node --test --test-concurrency=1 tests/editor/digitalTwinEffect.test.mjs tests/editor/digitalTwinEffect.integration.test.mjs tests/runtime/targetModelEffects.test.mjs tests/runtime/spatialEffects.test.mjs tests/runtime/digitalTwinEffectsRuntime.test.mjs tests/runtime/sceneEnvironmentEffects.test.mjs
npm run build
node scripts/smoke-digital-twin-effects.mjs
node node_modules/electron/cli.js tests/digitalTwin/digitalTwinEffectsPackages.integration.mjs
node scripts/smoke-digital-twin-effects-viewer.mjs
```

2026-09-21 本地验证覆盖 37 类新类型的 Chrome WebGL 渲染、Standard/PBR 着色器像素差异、编辑器属性与非法草稿、保存重开、模型绑定、运行预览及退出、复制重映射、静态合批隔离、零速度与零透明度、资源释放、实际 SOURCE/DIST ZIP 和 DIST Viewer 可见动画。输出位于 `output/playwright/digital-twin-effects/`，模型像素对照位于 `output/playwright/target-model-effects/`。预览仅使用本地 MQTT 握手 fixture，Viewer 仅模拟平台启用配置；没有连接生产 MQTT 或部署到线上项目。

原有广域检查有两个已存在的失败，本次保留其断言及无关实现：

- `tests/digitalTwin/projectLibraryTabs.test.ts` 的天空盒同步字符串断言在当前 HEAD 已不匹配；本次运行 18/19 通过，未修改 `ProjectPanel.tsx`。
- `test:encoding` 的严格 UTF-8 / 乱码扫描通过，但 Electron 文本入口检查报告 `digitalTwinPublishService.ts` 和 `localSceneResourceRecovery.ts` 的既有宽松读取调用；本次未修改这两个文件。
