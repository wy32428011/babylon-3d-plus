# 场景准备进度蒙版设计验收

prototype: `http://127.0.0.1:4174/`（验收完成后服务已关闭）
source: 用户提供的 ZENDING Logo 与场景准备进度需求

## 视觉

- ✓ 蒙版覆盖完整编辑器视口，层级高于现有 Dialog/Popover。
- ✓ 原始 ZENDING Logo 作为品牌进度轨道，源文件与项目资产 SHA-256 一致。
- ✓ Logo 与独立进度条均使用蓝色填充，并与百分数同步。
- ✓ 阶段标题、百分数和当前工作说明清晰可见。

## 行为

- ✓ 首次打开场景时显示，模型同步、资源刷新、运行时加载与 Geometry 合批全部落定后消失。
- ✓ 同一场景再次发生模型同步和资源刷新时重新显示，并在完成后再次消失。
- ✓ 蒙版期间鼠标与键盘编辑操作被阻止；冷启动实测按 `R` 不会切换旋转工具。
- ✓ 同步/刷新失败记录警告；运行时超过 120 秒时解除永久阻塞并写入 Console。

## 可访问性

- ✓ 使用 `role="progressbar"`、`aria-busy`、数值范围、百分数与阶段文本。
- ✓ 显示时焦点进入蒙版，完成后恢复原焦点；支持 `prefers-reduced-motion`。
- 手工屏幕阅读器完整走查未执行。

## 结论

READY — 用户要求的视觉、阶段门控、二次刷新与交互阻断均已通过自动化和真实浏览器验收。
